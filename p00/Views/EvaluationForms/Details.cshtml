@model p00.Models.EvaluationFormViewModel

@{
    ViewBag.Title = "تفاصيل الاستمارة";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

    <h2>@ViewBag.Title</h2>

<div  style=" margin: 0 auto; /* Added */
        float: none; /* Added */
        margin-bottom: 10px; /* Added */" class="col-10 card card-warning col-xs-10  "  ">
    <h4 class="card-header">معلومات الاستمارة</h4>
    <hr />
    <dl class="dl-horizontal card-body">
        <dt>
            @Html.DisplayNameFor(model => model.year)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.year)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.iscurent)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.iscurent)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Sections)
            <h3 class="fc-title">المحاور</h3>
        </dt>

        <dd>
            @foreach (var item in Model.Sections)
            {
                if(item.Checked)
                { <div>محور  @item.SectionName</div>}
                <br />
            }
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
